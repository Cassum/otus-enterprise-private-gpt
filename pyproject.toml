[project]
name = "enterprise-private-gpt"
version = "0.1.0"
description = "Enterprise Private GPT RAG system"
requires-python = ">=3.13"
dependencies = [
    "langchain>=1.3.14",
    "langchain-text-splitters>=1.1.2",
]
